{
  "name": "agent-factory-sdk",
  "version": "0.1.0",
  "description": "The frontend typescript SDK for interfacing with the MARCo Health Agent Factory",
  "main": "build/index.js",
  "author": "Jacob Boyle <teammarcorobot@gmail.com>",
  "license": "MIT",
  "private": true,
  "files": [
    "build"
  ],
  "type": "module",
  "dependencies": {
    "axios": "^1.7.7",
    "events": "^3.3.0"
  },
  "devDependencies": {
    "@google/genai": "^1.19.0",
    "firebase": "^12.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "prepare": "yarn build",
    "example": "yarn build & node build/example.js",
    "example:dev": "tsc & node build/example.js",
    "test": "yarn build & node build/test.js",
    "prettier:fix": "prettier --write '**/*.{ts,js,json,md}'",
    "prettier:check": "prettier --check 'src/**/*.{ts,js,json,md}'"
  }
}
